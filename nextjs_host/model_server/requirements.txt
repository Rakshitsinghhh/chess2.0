# ─── Web framework ────────────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.12

# ─── Chess + ML ───────────────────────────────────────────────────────────────
python-chess==1.999
tqdm==4.66.4

# ─── PyTorch (CPU-only — keeps Docker image under ~2 GB) ─────────────────────
# Install manually in Dockerfile:
#   pip install torch==2.2.2+cpu --extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.2

# ─── Optional: download model weights from HuggingFace Hub ───────────────────
huggingface_hub==0.23.4
